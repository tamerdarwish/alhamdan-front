import React from 'react';

const ProductDetailsPage = ({ match }) => {
  return (
    <div>
      <h1>Product Details</h1>
      {/* Display product details based on product ID from the URL */}
    </div>
  );
};

export default ProductDetailsPage;
